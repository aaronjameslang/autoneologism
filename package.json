{
  "name": "markov-chain-word-generator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "format": "standard-format --write",
    "lint": "standard --fix",
    "test": "tape test/*.js test/**/*.js | faucet",
    "test-debug": "tape test/*.js test/**/*.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "faucet": "0.0.1",
    "standard": "^7.1.2",
    "standard-format": "^2.2.2",
    "tape": "^4.6.0"
  }
}
